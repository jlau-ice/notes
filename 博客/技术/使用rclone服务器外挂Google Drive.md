前不久购入了一台16H16G的服务器，111三年的价格实在是香。是个小厂的服务器，所以有点担心跑路，所以需要对一些重要的数据做备份，这里我用到了 rclone 将google drive 外挂为服务器的硬盘。
一些重要的数据就都放外挂的中国目录里面了。
下面是具体操作步骤。

首先安装 rclone。由于服务器没有浏览器。所以本地需要安装rclone 。来获取google drive 授权的tocken。


```shell
# Arch Linux 


# Debain 



```

