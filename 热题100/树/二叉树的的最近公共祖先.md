253 236 
这两题，第一个简单，是个二叉搜索树，一下子就想到了
直接迭代
如果当前指针都大于 pq,则左移动。
都小于 右移动，如果 一个大一个小则 直接返回当前节点

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode(int x) { val = x; }
 * }
 */

class Solution {
    public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {
        TreeNode cur = root;
        while (cur != null) {
            if (cur.val > q.val && cur.val > p.val) {
                cur = cur.left;
            } else if (cur.val < q.val && cur.val < p.val) {
                cur = cur.right;
            } else {
                return cur;
            }
        }
        return null;
    }
}
```
